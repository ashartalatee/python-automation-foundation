# 🧠 Day 3 — Condition & Logic Thinking

## 1️⃣ Kenapa ini penting
- Semua automation bergantung pada *decision logic*.
- “If–Else” adalah dasar *control flow* dalam sistem otomatis.
- Di dunia nyata, setiap sistem automation harus *memilih tindakan* berdasarkan data.

## 2️⃣ Dasar Control Flow Python
| Struktur | Kegunaan | Contoh |
|-----------|-----------|--------|
| if | Menjalankan blok jika kondisi benar | `if x > 10:` |
| elif | Alternatif kondisi tambahan | `elif x == 10:` |
| else | Default (jika semua kondisi salah) | `else:` |
| and / or / not | Kombinasi logika | `if a and not b:` |

## 3️⃣ Contoh di Dunia Bisnis
| Kasus | Logika |
|-------|---------|
| Auto Email Reminder | `if invoice_due < today: send_email()` |
| Fraud Detection | `if amount > limit and country != 'ID': flag()` |
| Data Validation | `if not data_complete: skip_row()` |

## 4️⃣ Catatan
- Automation bukan sekadar “jalankan perintah” → tapi “ambil keputusan yang tepat”.
- Latih logika seperti sistem: data → kondisi → aksi.
